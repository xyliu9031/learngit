## 1
管道channel  本质上是一个队列，先进先出
数据是先进先出
自身线程安全 多协程访问 不需要加锁

var 变量名 chan 数据类型
管道是引用类型，必须初始化才能写入数据，即make后才能使用


## 2
管道可以关闭，关闭后不可以写 只可读

## 3
管道支持for - range 的方式进行遍历

## 4

select case 解决多个管道

## 5 defer recover